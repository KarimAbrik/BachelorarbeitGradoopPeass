@Override
public Vertex map(String csvLine) throws Exception {
    String[] tokens = split(csvLine, 3);
    return vertexFactory.initVertex(GradoopId.fromString(tokens[0]), tokens[1], parseProperties(CSVConstants.VERTEX_TYPE, tokens[1], tokens[2]));
}