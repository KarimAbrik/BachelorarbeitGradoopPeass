/**
 * {@inheritDoc}
 */
@Override
public GD readGraphHead(final Result res) {
    GD graphHead = null;
    try {
        graphHead = graphHeadFactory.createGraphHead(readId(res), readLabel(res), readProperties(res));
    } catch (IOException e) {
        e.printStackTrace();
    }
    return graphHead;
}