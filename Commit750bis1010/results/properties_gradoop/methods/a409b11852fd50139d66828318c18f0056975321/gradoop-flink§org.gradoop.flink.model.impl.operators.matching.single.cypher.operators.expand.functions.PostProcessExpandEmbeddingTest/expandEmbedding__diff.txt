private ExpandEmbedding expandEmbedding() {
    Embedding base = new Embedding();
    base.add(a);
    base.add(b);
    base.add(c);
    return new ExpandEmbedding(base, GradoopId.get(), GradoopId.get(), GradoopId.get(), a);
}