@Test
public void getEdgesByLabel() throws Exception {
    GradoopTestUtils.validateGraphElementCollections(Sets.newHashSet(e0), from(asList(g0, g1), asList(v0, v1, v2), asList(e0, e1)).getEdgesByLabel("a").collect());
}