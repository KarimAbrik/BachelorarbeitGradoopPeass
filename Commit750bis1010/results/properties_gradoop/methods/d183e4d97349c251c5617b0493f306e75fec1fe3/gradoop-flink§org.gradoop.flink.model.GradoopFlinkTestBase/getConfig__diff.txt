/**
 * Returns the configuration for the test
 *
 * @return Gradoop Flink configuration
 */
protected GradoopFlinkConfig getConfig() {
    if (config == null) {
        setConfig(GradoopFlinkConfig.createConfig(getExecutionEnvironment(), getGraphLayoutFactory(), getCollectionLayoutFactory()));
    }
    return config;
}