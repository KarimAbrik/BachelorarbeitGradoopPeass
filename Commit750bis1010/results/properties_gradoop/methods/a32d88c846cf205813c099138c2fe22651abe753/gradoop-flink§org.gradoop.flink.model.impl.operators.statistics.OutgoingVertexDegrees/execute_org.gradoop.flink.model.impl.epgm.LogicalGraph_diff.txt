@Override
public DataSet<WithCount<GradoopId>> execute(LogicalGraph graph) {
    return new EdgeValueDistribution<>(new SourceId<>()).execute(graph).rightOuterJoin(graph.getVertices().map(new Id<>())).where(0).equalTo("*").with(new SetOrCreateWithCount());
}