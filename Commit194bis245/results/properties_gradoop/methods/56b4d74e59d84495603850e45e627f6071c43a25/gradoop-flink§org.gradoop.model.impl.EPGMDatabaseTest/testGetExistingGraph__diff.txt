@Test                                                                                                  @Test                                                                                               
public void testGetExistingGraph() throws Exception {                                                  public void testGetExistingGraph() throws Exception {                                               
LogicalGraph&lt;DefaultVertexData, DefaultEdgeData, DefaultGraphData&gt; g = getGraphStore().getGrap   LogicalGraph&lt;DefaultVertexData, DefaultEdgeData, DefaultGraphData&gt; g = getGraphStore().getGrap
assertNotNull("graph was null", g);                                                                    assertNotNull("graph was null", g);                                                                 
assertEquals("vertex set has the wrong size", 3L, g.getVertices().size());                           | assertEquals("vertex set has the wrong size", 3L, g.getVertices().count());                         
assertEquals("edge set has the wrong size", 4L, g.getEdges().size());                                | assertEquals("edge set has the wrong size", 4L, g.getEdges().count());                              
assertEquals("wrong label", LABEL_COMMUNITY, g.getLabel());                                            assertEquals("wrong label", LABEL_COMMUNITY, g.getLabel());                                         
}                                                                                                      }                                                                                                   
